# Build context for the API image is services/api. Docker reads the .dockerignore at
# the root of the context, so the repo-root one does not apply here.

.venv
**/__pycache__
*.egg-info
.pytest_cache
.mypy_cache
.ruff_cache
tests
.env
.env.*
